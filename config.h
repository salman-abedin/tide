static char* server[] = {
    "192.168.0.101", /* remote address (Don't comment out) */
    "8022",          /* remote port (Don't comment out) */
    "local", /* comment out for remote access, comment in for local usage)*/
};
